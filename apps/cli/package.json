{
  "name": "@spark-apps/cli",
  "version": "0.1.0",
  "description": "CLI tool for Unity MCP automation",
  "type": "module",
  "bin": {
    "unity-mcp": "./dist/index.js"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "format": "prettier --write \"src/**/*.ts\"",
    "test": "jest --passWithNoTests"
  },
  "dependencies": {
    "@spark-apps/unity-mcp-server": "workspace:*",
    "commander": "^12.1.0",
    "chalk": "^5.4.1",
    "ora": "^8.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.17.24"
  }
}
